# 安全审计 R3：输入面对抗（ITERATION 5 / W-009）

- **日期**：2026-09-18
- **审计方式**：零上下文 security-auditor 子代理（只读命令：git grep / 脚本自测 / JS 门 / SBOM 对照；禁 flutter 命令与密钥文件）
- **覆盖标签**：ADVERSARIAL + BOUNDARY_CONTRACT（较 iter4 SCALE_DATA 升级）
- **范围**：桥 handler ×8、注入 JS、分片重组、`LinkBuilder`、更新链、日志/诊断脱敏、擦除覆盖清单、Android 面、依赖与豁免
- **门禁（审计时）**：`gates.sh i5 --osv --strict-state` 11 道门 PASS（OSV BLOCKED）；test 646/646

## 总评

**风险等级：中。** 未发现可越出桥信任边界（令牌 + URL 双门禁）拿凭证或伪造高权限消息的路径。主要缺口是无界内存（两个 P2：能从"页面自伤"升级为"进程级击杀"）与若干纵深防御收尾（P3）。

## 发现与处置

| # | 级别 | 问题 | 构造输入（摘要） | 处置 |
|---|---|---|---|---|
| S-1 | **P2** | `SessionIndex` 与 `StateDiffer._prev` 无界：4 MiB 帧带 ~10 万唯一 sessionId，数十秒灌到百万条 → OOM/ANR，进程级击杀 | 持令牌循环 `callHandler('zrEvents', [1e5 个唯一 id], token)` | **已修**：单设备表上限 5000、超限按插入序压到 7/8；`_prev` 上限 10000；回归×2 + 变异×2（D-20260918-07） |
| S-2 | **P2** | 深度炸弹：一条 4 MiB 消息 ~4e6 层嵌套，jsonDecode 途中数百 MB 瞬时分配（纯 relay 帧也可触发） | `'[' * (4MiB-1)` | **已修**：`BridgeMessagePipeline` 解析前 O(n) 深度预扫（max 64，字符串内不计数），超限整帧丢弃；回归×3 + 变异×2（D-20260918-07） |
| S-3 | P3 | hookScript 分片 assembler 用普通对象，`logicalFrameId: "__proto__"` 原型污染（node 实证），破坏官方页脚本 | relay 帧 `logicalFrameId: "__proto__"` | **已修**：`Object.create(null)` + id 白名单正则 `^[A-Za-z0-9_-]{1,128}$`；源码断言 + 变异（D-20260918-08） |
| S-4 | P3 | `JumpOutcome/InPageBackOutcome.parse` 的 id 用 `num`，`1e400`/NaN 过检后 `toInt()` 抛 UnsupportedError 逃出 handler，回执丢失与看门狗竞态 | `callHandler('zrJump', '{"id":1e400,…}')` | **已修**：`is! int`；回归 + 变异（D-20260918-08） |
| S-5 | P3 | 令牌向任意主文档无条件注入（Android POST 主框架导航不经 shouldOverrideUrlLoading，非官方文档短暂驻留高权限容器可截获令牌）；`_bridgeAllowed` 的 URL 复核存在异步 TOCTOU | POST 到 evil.com → 收集令牌 → 回官方页 | **已修（主缓解）**：令牌/主题只注入 `isTrustedRemotePage` 的文档——无令牌的文档在 `_bridgeAllowed` 已是死门，TOCTOU 失去标的；行为级变异豁免需 WebView（语料显式登记）（D-20260918-08） |
| S-6 | P3 | `LogRedactor.route` 无长度上限：2 MB path × 环形 500 行 ≈ 1 GB 堆 | `location.href = 官方域 + 'A'*2e6`（被拦记 WV103） | **已修**：`maxRouteChars=256`；回归 + 变异（D-20260918-08） |
| S-7 | P3 | 通知标题不截断（正文 180 对称缺失）：4 MB 标题走系统通知通道 + `DeviceFeed` + UI | 帧内 4 MB title + 审批计数 | **已修**：`titleFor` 截 120；回归 + 变异（D-20260918-08） |
| S-8 | P3 | SessionJump 子串点击：页面把 taskId 设成敏感控件 testid 子串，通知 tap 被劫持为"点击任意元素" | `taskId: "permission-approve"` + 用户点通知 | **未改代码**，登记候选 W-016（需与页面 testid 契约对齐，单独小批） |
| S-9 | P3 | 安全不变量测试对 UserScript origin 断言弱（`>=3`，实际 4 个脚本） | — | **已修**：数量相等且 ≥4 |

## 观察（无复现路径，摘）

O-1 zrSeen 双口径 512/256 KiB（存量偏差）；O-2 全部 Dart→JS 内插点已 jsonEncode，无脚本注入；O-3 擦除覆盖对照 **无凭证/会话内容遗漏**；O-4 更新链闭环，`_safeDownloadUri` 建议对 tag normalize+encode（需 repo 级攻陷）；O-8 Android 面（allowBackup=false、无 cleartext、无导出 deep link）成立；O-10 零宽/bidi 控制字符未过滤（内容层，记录）。

## 复核记录（返修后）

- 门禁：`gates.sh i5 --osv --strict-state` 11 道门 PASS / test **656/656**（646+10）/ 变异 iter5 语料 **8/8 caught + 2 exempt (declared)**（`evidence/i5-mutations.log`）
- 复核结论：见 `docs/audits/2026-09-18-security-r3-reverify.md`
