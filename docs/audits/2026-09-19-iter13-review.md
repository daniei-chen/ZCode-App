# ITERATION 13 评审与闭环记录

- 日期：2026-09-19
- 方式：dynamic-workflow 编排——3 个并行修复子代理（文件互不相交）+ 只读 CI 观察员 + `world.run` 确定性门禁 + 零上下文独立复核员（一轮通过）+ 有界返修员（未触发）
- 范围：W-026（l10n 死键）/ W-030（刷新时钟口径）/ W-015（验证器 --require-tracked）/ 新仓库 CI 观察

## 复核员终审（第 0 轮通过）

三件改动代码本体全部成立：184 个 l10n 死键净删零漏网引用（静态词边界扫描 + 导出目录 analyze 双重确认）、双钟置位/清空严格成对且 relockEvidence 取大语义正确（负墙钟差被吸收、staleAfter=60s 下测试场景期望成立）、iter13 语料 find 唯一/only 语义/覆盖闭环合规、--require-tracked 的非 git 降级与 test_* 夹具豁免正确。

## 复核注记（5 条）与本轮闭环

| # | 注记 | 级别 | 处置 |
| --- | --- | --- | --- |
| F-1 | 变异全语料无 i13-mutations.log（gates 不含 corpus 实跑；iter13 两条新变异的回退即失败实跑不可见，仅静态验证） | P2 | **已闭环**：`mutate.py --export ci_i13 --spec iter1..iter13 --require-green` 全语料重跑，结果见 `evidence/i13-mutations.log`（100/102 caught + 7 exempt 量级，以 log RESULT 行为准） |
| F-2 | 新增 10 条 RequireTrackedChecks unittest 无门禁执行（selftests 段只跑 scripts/*，沿袭缺口） | P2 | **已闭环**：`python -m unittest discover -s tools/iteration -p "test_*.py"`（31 条全绿）写入 gates.sh selftests 段，随 i13 门禁执行 |
| F-3 | W-026 的 session_index 半边未处置：`'未命名工作区'` 硬编码仍在，SessionGrouping 在 lib 内零调用方（仅测试引用） | P2 | **已闭环**：SessionGrouping 整类删除（workspaceKey/workspaceLabel/groupByWorkspace/groupByDay/_keyOf/_startOfWeek，含硬编码文案），session_index_test 对应两组测试随删；SessionRanking/RelativeTime 保留（同样 lib 内零调用但被测试钉住、无硬编码文案，属 v1.1.0 会话列表预留，已在台账注明） |
| F-4 | 死键计数与审计口径差 5（189 vs 184） | P3 | **已解释**：189 是 iter12 清扫的两遍粗 regex 估计；184 是逐键词边界扫描的精确值。差额 5 = 4 个被"原始键名兜底扫描"救活的接收者形态键（`l.notifChannelApproval`、`l10nZh.notifTestBody` 等）+ 1 个删后按复核加回的 `settingsGroupNotifications`。精确值 184 为准 |
| F-5 | `diagnosticsUpdateDownloadable/Manual` 带 `{version}` 占位符但缺 @metadata（HEAD 遗留，非本轮回归） | P3 | **已修**：补 `@…{"placeholders":{"version":{"type":"String"}}}` 两条并重新 gen-l10n（生成签名 `String version`） |

## 新仓库 CI 观察（只读 API，全部 verified）

- ci.yml：run#35451826990（dependabot PR）与 run#35451776952（main push）均 **success**
- ci-heavy.yml：run#35451826982 **success**
- release.yml：run#35453136471（tag v1.0.0 触发）**failure**——verify job 的 step#5 "Verify provenance (tag version + commit must come from protected main)" 失败，build-sign/publish 跳过。原因：工作流设计为仅从受保护 main 发版，新仓库未配置分支保护与签名 Secrets（发布链由本地构建路径承担，见 E-48）
- 附：dependabot-updates 工作流 2 条 run 均 success

## 验证

- `gates.sh i13` 全量门禁（导出目录）：由 world.run 直接执行，退出码裁决
- `mutate.py --require-green` 全语料（iter1–iter13）：独立实跑归档
- `python -m unittest discover tools/iteration`：31/31
- 独立复核：零上下文、一轮通过；5 条注记逐条闭环如上

## 未覆盖

- 真机深睡/回拨场景（仅单测 + 源码钉）
- release.yml 自动出包链（依赖用户在新仓库配置签名 Secrets 与分支保护 → W-031）
