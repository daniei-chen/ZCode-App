# 代码评审：ITERATION 4 批次（W-004 模拟数据标注 + W-008 规模测试）

- **日期**：2026-09-18
- **评审方式**：零上下文 code-reviewer 子代理（**新代理**，与 iter3 评审员不重叠；只读）
- **覆盖标签**：DIFF_CORRECTNESS + **SCALE_DATA**（较 iter3 升级）
- **评审对象**：`D:\tmp\zr\iter4_batch.patch`（对 iter3 基线导出 `/d/tmp/zr/ci_i3`，4 文件：`test/scale_test.dart`、`tools/iteration/mutations/iter4.json`、`docs/BENCHMARKS-RESULTS-v1.0.8.md`、`docs/PROJECT_AUDIT.md:68`）
- **门禁（评审前）**：`gates.sh i4 --osv` 10 道门全 PASS（OSV BLOCKED）；test 646/646（+11，套件墙钟 49 s vs 48 s）；变异 19/19

## 结论

**需返修后合并**（1×P1 + 1×P2 + 6×P3）。W-004 本身通过；W-008 的问题集中在"变异覆盖不完整"与"断言偏弱"。

## 发现与处置

| # | 级别 | 位置 | 问题 | 处置 |
|---|---|---|---|---|
| F-1 | **P1** | `mutations/iter4.json` | 11 条新测试只有 7 条被变异钉住；纲领 §1 要求"每个新测试"，批次却声称"每组至少一条"；feed 用例若回退成 `putIfAbsent` 照绿 | **已修**：+4 条变异（窗口判断忽略 / feed 首值生效 / unread 不累加 / 诊断包告警不派生）；8 KiB 常量用例无有意义变异 → `mutate.py` 新增 `exempt` 语义，语料里**显式登记豁免与理由**（纲领 §3 W-008 同步口径） |
| F-2 | P2 | `scale_test.dart` vs 纲领 §3 W-008 | Stopwatch 20 s 绊线是墙钟绝对值，与验收文字"不用墙钟绝对值"字面冲突；但取舍成立（三个数量级余量，且实证抓住 291 s 的二次方退化） | **已修**：纲领 W-008 验收改为"结构断言为主；允许每条一道 20 s 级灾难性退化绊线" |
| F-3 | P3 | gate 1e5 用例 | "最早键被淘汰"依赖 `DateTime.now` 平局排序（推演后实际零风险） | **已修**：注入单调假时钟，淘汰顺序完全确定 |
| F-4 | P3 | feed 用例 | 未钉住 pendingTotal"末值生效"：首值/末值同为 `1+k%5`，putIfAbsent 回退照绿 | **已修**：计数改为随批次递增（首 1 末 5），逐键精确断言 == 5；配变异 `iter4-feed-first-value-wins` |
| F-5 | P3 | dedupe 幂等断言 | 只比 taskId 序列 | **已修**：整列表恒等比较（唯一键保留原实例） |
| F-6 | P3 | LogRedactor 用例 | "1 MiB"名不符实（0.71 MiB）；输出上界偏松 | **已修**：6000 轮 ≥1 MiB；上界收紧为 `< input.length`（本夹具所有替换只缩短） |
| F-7 | P3 | 诊断包 1000 设备夹具 | `device-0000-xxxx` 经 shortId 前 8 位只剩 10 个可区分 id | **已修**：`d0000000..d0000999` 两两不同 + 断言集合大小 1000 + 告警内容断言（OB201/OB205） |
| F-8 | P3 | BENCHMARKS 横幅 | "只含模拟器数据"与 §被测产物的真实溯源信息有歧义；日期 2026-09-18 与评审员时钟（09-17）不一致 | 措辞**已修**为"所有测量数据均为模拟器数据；APK 溯源信息是真实的"；日期按主代理系统时钟（2026-09-18）保留 |

## 评审员核实记录（摘要）

- 6 条原有变异 find 串均为目标文件唯一出现；expect_fail 与真实测试名逐一匹配；`only` 语义可判定。
- 字节边界用例选取精准：`'中'` 3 字节恰卡上限、`'😀'` length 152 < 300 而字节 304 > 300，精确复现 F04 形态。
- 夹具自检（先断言 `utf8.encode(exact).length == maxBytes` 再测实现）避免假绿。
- W-004：横幅与 A-12 逐点对应；空模板分节确实全空；全文无未标注的"看似实测"数字。
