# ITERATION 15 评审记录（W-034 待处理中心）

- 日期：2026-09-19
- 来源：升级路线图自主层第 2 项（"应用内待处理中心：事件历史 + 会话待处理速览"，run `dwfrun-3d9662c8`）
- 复核：两轮零上下文 code-reviewer（初审需返修 → 返修后新代理终裁可合并）

## 变更

- `lib/state/event_history.dart`（新）：按设备内存态有界事件历史（50/设备，新在前）；字段清洗（bidi 剥离 + 120 码元截断）；taskId 按跳转白名单上限截 128；`latestPendingRequest(entries, pendingKeys)` 与权威计数交叉校验。
- `lib/ui/pending_center_sheet.dart`（新）：「等待处理」（EventFeed.permPending，计数求和，点按=切设备+markRead+仍在等待才设置会话跳转）与「最近事件」（历史，复用 RelativeTime 相对时间）；空态；红点等待行。
- `lib/ui/manage_page.dart`：头部通知铃入口（permPending 驱动红点）。
- `lib/services/webview_sync.dart`：resolved/enabled 两处收口旁记录历史；forget 同生命周期。
- `lib/state/protected_wipe.dart` / `lib/state/session_pool.dart`：擦除/设备删除清历史。
- `lib/ui/app_shell.dart`：`ref.listen(pendingSessionJumpProvider)` 跳转到达即揭启动器盖板。
- l10n +15 键；测试 14 例（event_history 8 + pending_center 6）；变异 `iter15.json` 18 条。

## 初审（A-128，需返修）

3×P2 + 4×P3，核心数据路径经逐项核对无问题：

1. **F1**：启动器下点按"当前设备"的等待行——`activeTab.set` 同值不发射（Riverpod `!=` 语义），揭盖监听不触发，用户无可见导航（跳转在盖板后执行）；且漏 markRead。
2. **F2**：`latestPendingRequest` 只取"最新审批类"，不与权威键交叉校验——resolved 不删历史条目，可跳到已解决的旧任务（等待行计数与跳转目标来自不一致数据面）。
3. **F3**：擦除/删除清历史无测试与变异哨兵（历史含标题/摘要，属 R-04 红线面）。
4. P3：入口红点无断言；taskId 无长度上限；面板时间不刷新（polish）；i15 证据未覆盖本批。

## 返修与终裁（A-129，可合并）

- F1：AppShell 增跳转监听揭盖（与 `_jumpTo`/`_openDevice` 同口径）；面板补 markRead。终裁复核确认不会漏（Riverpod 通知内各 listener 收到同一快照值；官方页 clear 不阻断 AppShell 收到非 null）。
- F2：交叉校验 `pendingKeys.contains(taskId)`；测试补 resolved-后命中更旧等待项、同 taskId 非审批类不得选中（后者使 type 检查可观察，杀死原本被掩体掩护的变异）。
- F3：补「擦除事务清空历史（R-04）」与「删除设备清历史」（复用 R-6 secure-storage mock 骨架）+ 两条变异。
- P3：入口红点补断言+变异；taskId 截断+测试+变异。
- 过程如实记录：launcher 源码钉最初撞上文件别处同名单行（变异存活），改"监听头+两行序列"断言；两次 Dart 转义事故（`\r\n` 经 python heredoc 变真实换行）用 Edit 工具直接修复（L-26 复现）。
- **放行条件**：冻结快照复跑全量变异确认 127/127（首跑 125/127 两条异常与本批无关——event_feed/page_refresh_policy 目标测试手动复现均能被捕获，疑为并发/构建缓存瞬时伪影）；见 `evidence/i16-mutations.log`。

## 验证

- `gates.sh i16`：10 门全 PASS，test **724**。
- `iter15.json` scoped 实跑 **18/18 caught**；全语料终跑归档见 `evidence/i16-mutations.log`。
- 终裁复核独立核对：18 条 find 唯一性、expect_fail 精确匹配、only 失败数推演与实跑吻合、两处源码钉 EOL 归一正确、边界（markRead 对空 feed 安全、负 atMs 容忍、taskId 非法字符最终被跳转白名单拒绝）无问题。

## 未覆盖 / 下轮

- 真机视觉与 TalkBack 实读（语义/布局仅静态+widget 测试）。
- 面板相对时间不自动刷新（P3 polish，未做）。
- 两条可选断言（sheet 等待行红点、sheet 内 markRead）登记 W-035。
- mutcov testWidgets 盲区（W-033）仍待工具级修复。
