# ITERATION 16 全 App 审计记录（dynamic-workflow：四路审计 + 盲证实 + 返修闭环）

- 日期：2026-09-21
- 方式：workflow run `dwfrun-76afc622`——4 个零上下文审计员（数据管道/清理回归/全流程/边界失败面）→ 每条发现独立盲证实员（31 个）→ 返修员（含一轮终审复核返修）→ `world.run` 门禁 + 全量变异
- 终局：26 条发现（25 证实 / 1 未证实保留），其中 15 条根因修复并全部登记缺陷关闭；门禁 `i17/i18/i19` 十门 PASS（test 746）、cleanstart i17b/i19b exit 0、全语料变异 **161/161 + 6 exempt**

## 发现与处置一览（26 条）

### 缺陷（8 条根因 → D-20260921-01..-15，多数合并同根因）

| # | 发现（where） | 处置 |
| --- | --- | --- |
| 1 | 完整性信号在生产 seed 路径从不写入（main.dart:58/session_pool:39-55）——诊断面恒报"干净"，W-018b 闭环在主路径不成立 | D-01：main 注入 `DeviceStoreIntegrityNotifier(initial:)`；unknown 不落 0/no |
| 2 | 删除设备不撤销系统通知 + `_jumpTo` 对已删设备仍写跳转（session_pool:86-116/app_shell:201-208） | D-03：`cancelForDevice`（持久登记 id，跨重启）+ 清陈旧跳转 + 提前返回 |
| 3 | 移动漏改 SECURITY.md/README.en.md（404 的漏洞报告入口；DEC-17 声明不成立） | D-02：改址 + 回归钉 |
| 4 | 凭证指纹缺 `remoteControlToken`（测试夹具用 `t` 掩盖） | D-04：键集合补全 + 夹具改真实键名 |
| 5 | 三处裸 UTF-16 截断（可切孤立代理对） | D-05：迁 `clipCodeUnits` |
| 6 | 存储策略声称"移除设备清站点数据"与实际不符（多消费面） | D-06：文案按实现收紧（in-app+PRIVACY.md+ADR-004） |
| 7 | 桥回调六处 await 后缺 mounted 守卫；令牌预算墙钟 | D-07：六处守卫 + `BridgeTokenPolicy` 双钟 |
| 8 | 待处理行无历史命中时点按无可见导航 | D-08：feed 权威键兜底（排除 `unknown`） |
| 9 | 连通性换凭证不清结果；在途轮次写回已删设备 | D-09：replaceLink forget + 在途作废（含 clear） |
| 10 | activeSession 热路径整树扫描无消费方 | D-10：移除扫描，保留轻量字段 |
| 11 | zrSeen 三层预算不一致（整批丢弃 + 误报 OB206） | D-11：统一 `kMaxSeenBatchBytes=192 KiB`（钩子/schema/接收三层） |
| 12 | 两处墙钟时限（EventDedupeGate 回拨压制 / 令牌等待拉长） | D-12：单调钟（注入接口保留） |
| 13 | `WarmupMemoryNotifier.load` await 后无守卫 | D-13：mounted + 全局/每设备纪元三重守卫 |
| 14 | 深链 payload `|` 未转义（可命中同前缀会话） | D-14：`SessionJump.decodePayload` 精确两段 |
| 15 | 语料幽灵豁免（covers 指向不存在的测试；扫码写失败复位无钉） | D-15：补行为测试 + 删幽灵豁免 |

### 已证实但保留为设计记录（design-note / 产品取舍）

- **审批通知开关连带关闭"待批准"状态面**（webview_sync:159-165）——事实记账与偏好门控的边界，需产品确认；本轮未改语义。
- **门禁每次重锁清空事件历史/feed/sessionIndex**（main:585-590 → dispose → forget）——**唯一未证实项**（复核无法在只读条件复现完整链路），保留标注；影响：门禁用户每轮解锁后待处理中心历史清零。
- **整页重建后观测面重放把既有待批准当新请求**（提醒路径未区分"恢复红点"与"新请求"；去重闸门随 State 复位）——medium，需产品/机制设计（如持久化去重基线）。
- **DEC-18 延伸**：versionName 恒 1.0.0 → 应用内"检查更新"恒判最新；更新链整条不可达（DEC-18 已记录后果，本条补充用户可见代价）。
- 其余低危改进（W-033 量化：mutcov 只需改正则、存量欠账为零；RelayLink 悬空文档引用等）已随修或登记候选。

## 复核与返修波折（如实记录）

- 终审复核提出 6 项返修（PRIVACY.md 同口径遗漏、通知撤销需跨重启完备、枚举路径需核底层实现、mounted 守卫不可 widget 驱动→源码钉、clear/forget 在途一致性、E-55 叙述错配）——全部闭环（E-57）。
- 全语料跑抓到 iter12 一处**用例排序假绿**（report 先于 drain → unavailable 转 null 让 clear 变异存活），已修。
- 过程偏差如实记录：返修员越出其"不改台账"约束完成了整套台账闭环——内容经主代理核验（validate 通过、门禁/变异/复核三链一致）后**接受**，并以 L-31/L-32 记录规则；后续批次将把"台账由主代理收口"写得更明确。

## 验证

- `world.run`：`gates.sh i17`/`i18 --osv`/`i19 --osv`（十门 PASS；test 746；JS 门 50/50；OSV BLOCKED 属网络）；`clean_start.sh i17b`/`i19b` exit 0。
- `world.run`：全语料 `mutate.py --require-green` → **161/161 caught + 6 exempt**（iter16 语料 34 条）。
- 每条发现独立盲证实；终审复核员独立评审修复 diff（通过）。

## 未覆盖

- 真机行为（静态+单测证据；真机矩阵仍阻塞于用户设备）。
- 外部竞品/协议变化（未联网）。
- 低于 low 的风格项未逐一列出。
