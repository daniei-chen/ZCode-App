# ITERATION 14 评审记录（W-032 未读/待批准语义分离）

- 日期：2026-09-19
- 来源：升级路线图工作流（run `dwfrun-3d9662c8…`）top1 推荐；缺陷登记 D-20260919-16（P2）
- 复核：零上下文 code-reviewer（A-126 初审 / A-127 终裁）

## 变更

- `lib/ui/unread_badge.dart`：未读数与「待批准」语义分离——有未读 → 计数徽标（待批准时危险色）；仅剩待批准 → 10px 危险色圆点常显（带语义标签 `badgePendingApproval`）；都没有 → 不渲染。旧实现 `unread<=0` 整体隐身，打开设备 markRead 后仍在等待的审批红点被一并抹掉。
- `lib/state/event_feed.dart`：注释按新语义修订——resolved 剩余量建立的条目会让卡片常显红点，属有意行为（事实记账，非未读提醒），随 R-19 复审条件。
- `lib/l10n/app_zh.arb`：新增 `badgePendingApproval`（+元数据），gen-l10n 同步。
- 测试：`test/unread_badge_test.dart`（6 例）；`test/event_feed_test.dart` 增 resolved 建条目钉（重复的 markRead 钉已删——既有用例已覆盖同语义）。
- 变异：`tools/iteration/mutations/iter14.json` 7 条（6 用例逐条守卫 + markRead 行为钉）。

## 复核时间线

1. **A-126（初审）：需返修**——范围在测试与语料，产品代码无阻断项：
   - P1-1：`iter14-badge-cap-removed` 语料自坏（replace 使 2 个测试失败但 only 期望 1），且不钉 cap（t5 照过）。
   - P1-2：t2/t4/t5/t6 无回退变异；mutcov 因只扫 `test(` 对全部 testWidgets 用例空过（假绿）。
   - P2：`event_feed.dart:139-144` 注释被本轮证伪（"红点 UI 只在 unread>0 时显示"）。
   - P3：红点颜色/尺寸与 cap 边界不被钉、语义标签仅红点态、对比度存量项等。
2. **返修**：cap 变异换 `'$unread'`；六用例各配干净单变量变异（并实跑抓到两处静态推演遗漏：empty-shrink 变异同时杀 t6 → expect_fail 补第二条；t3 断言 Semantics/Container 父子方向修正为 descendant）；注释更新 + 语义钉；mutcov 盲区登记 W-033（L-30）。
3. **A-127（终裁）：可合并**——两条 P1 按处方修复且推演/证据自洽；P2 注释与语义钉落地；残留备注（event_feed_test 属 legacy、不在 coverage-files）已按计划以行为变异（markRead 清 pending）与 W-033 入账。

## 验证

- `gates.sh i15` 10 门全 PASS、test **710**（含新增用例）。
- `iter14.json` 7 条变异全部"回退即失败"；全语料 `--require-green` 归档见 `evidence/i15-mutations.log`。
- 复核独立核对：find 串唯一性、expect_fail 与 only 语义逐条推演、导出快照含返修后状态。

## 未覆盖

- 真机视觉验证（红点与计数徽标的排布在真机上的观感）；TalkBack 实读（语义标签已就位）。
- mutcov testWidgets 盲区的工具级修复（W-033，含存量欠账，单独一轮）。
