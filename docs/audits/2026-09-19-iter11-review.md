# 独立评审：ITERATION 11 批次（W-012/W-014/W-016/W-024）

- **日期**：2026-09-19
- **评审方式**：零上下文 code-reviewer 子代理（新代理；只读；对 cp-10 基线 7 文件 patch + 工作树实读，含 Flutter SDK / event_observer 计数来源 / RELAY-PROTOCOL 契约交叉核实）
- **覆盖标签**：DIFF_CORRECTNESS + BOUNDARY_CONTRACT + **CLEAN_START_RELEASE**（新标签，覆盖升级）
- **门禁**：`gates.sh i11 --osv --strict-state` 11 道门 PASS / test **700/700**

## 批次内容

1. **W-014**：OB202/OB203 分母命中数用 `cappedHits(hits, cap)` 封顶（WS↔framesDecoded、fetch↔fetchCloned；cap 缺席回退）。
2. **W-016**：跳转收紧——Dart `taskIdWellFormed`（`^[0-9A-Za-z_-]{1,128}$`，不合格走 invalid 分支）+ JS `find()` 从任意子串收紧为精确等于 tid 或 `task-item-`+tid 前缀。
3. **W-024**：连通性探测 `_inFlight` 去重（try/finally）。
4. **W-012**：`clean_start.sh` 冷启动门（fresh export → pub get → gen-l10n **arb↔生成物漂移检查** → analyze → 全量 test）。

## 首轮发现与处置

| # | 级别 | 问题 | 处置 |
|---|---|---|---|
| F-1 | **P1** | mutcov 门不可复现：新增测试 6 条中 1 条（"framesDecoded 正常…"不误报用例）无变异，但证据日志写 0 uncovered——证据与树不一致 | **已修**：补 `iter11-ws-total-dropped` 变异（`wsTotal = wsMiss` → 该用例恰好独红），mutcov 恢复可复现 PASS |
| F-2 | **P1** | clean_start.sh 的 `{ … } | tee` 管道子 shell 吞掉 FAILED，`exit $FAILED` 恒 0——门关不上 | **已修**：改单一写者（块重定向 + cat），退出码留在主 shell |
| F-3 | P2 | clean_start 证据是旧树跑的（694 vs 门禁 700） | **已修**：最终树重跑（`i11b`：700/700，exit 0） |
| F-4 | P2 | 日志多写者互踩（tee 截断 + >> 追加交错，已提交证据里时序错乱实锤） | **已修**：随 F-2 单一写者重构 |
| F-5 | P2（改注释） | W-014 注释声明"虚报的命中不会抬高分母"过强——页面可同帧伪造 framesDecoded | **已修**：注释改为如实边界（防的是最廉价灌大路径；主动伪造超出计数层，触发本身即信号）+ cap 绑定时阈值语义变化说明 |
| F-6 | P3 | cap 绑定时阈值语义漂移（合法无 base64 帧流量下更敏感）未记录 | **已修**：注释说明"min 只减不增，不漏报只可能更敏感" |
| F-7 | P3 | ignoredKeys 契约注释过时（framesDecoded/fetchCloned 实际被读取） | **已修** |
| F-8 | P3 | W-016 白名单 min 1 vs 登记 min 6 | **已修**：登记修订为实际实现并说明理由（官方 id 无最短契约；子串→前缀已是实质收窄） |
| F-9 | P3 | 两处注释与实现不符（invalid 分支看门狗措辞、W-012 描述含未实现的 debug build） | **已修** |
| F-10 | P3 | 跳过轮对设备清单变化不感知（可接受） | 维持，注释已声明"下一轮揭盖再触发" |

## 负向验证

冷启动门的漂移检查用**必败样**实测：故意改 arb 不重新生成 → `clean_start` exit 1 且报 `l10n DRIFT`（两个生成文件均报）→ 修复后 exit 0。F-2 的退出码契约由该实测背书。

## 结论

首轮：需返修后合并（F-1/F-2 两个 P1 必修）。返修与补登记落地后**最终裁定：可以合并**——"F-1..F-10 的处置经实读逐一验证属实：门禁证据链自洽、可由当前树复现，负向验证闭合了 clean_start 的失败路径，无需再返修。
