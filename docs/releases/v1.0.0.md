# v1.0.0 — 全新起点（新仓库首个版本）

> 版本：`1.0.0+20`。新仓库的版本号从 v1.0.0 重新开始；Android 内部版本号
> （versionCode 20）继续递增，已安装旧版（<20）的机器可直接覆盖升级，
> 数据不丢。签名密钥与历史正式版一致（证书指纹 `07091ffd…`）。

## 本版内容（相对上一交付版）

**1. 修复"正在加载工作区"卡片仍会露出（真机双端复现）**

上一版把隐藏器重写为 1 秒节拍扫描后，真机上卡片仍会显示。定位到根因：
扫描对**文本节点长度设了 120 字符上限**，而官方页把卡片正文（标题 + 说明 +
4 个步骤）渲染成**单个文本节点**（约 130–180 字符）——整段被跳过，永不隐藏。
（此前本地"仿制卡片"恰好约 75 字符，测试因此漏过。）

修复并加第二层保险：
- 文本节点上限 120 → **600**；
- 新增**屏幕九宫格命中测试**探测（`elementsFromPoint`）：卡片/覆盖层必然
  覆盖采样点，不依赖文本节点结构；
- 卡片判定仍要求命中 **≥2 个不同关键词**，避免误伤引用单个词的聊天正文。

验证（真实页面 + CDP，四种形态）：
| 形态 | 结果 |
| --- | --- |
| 短单文本节点卡片（约 75 字） | 已隐藏 |
| **超长单文本节点卡片（约 150 字，旧上限漏网场景）** | **已隐藏（修复生效）** |
| 多节点拆分卡片 | 已隐藏 |
| 聊天消息仅含一个关键词（防误伤） | 保持可见，不误伤 |

**2. 系统返回键：平板/大屏也走页内返回（口径修订）**

撤销"平板上跳过页内返回脚本"的规则：它导致 web 设置面板（如"插件"页）
按系统返回直接退出到设备页，与用户预期相反（用户要求：等同于点击页面
左上角的「返回工作区」）。现在手机与平板同一条路径：有页面自带返回控件
就点它；没有（对话+列表同屏页）则一次返回露出设备页。

**3. 安全门禁死锁修复（随本版一并发布）**

读取安全设置失败时锁屏提供「清除安全设置并继续」（二次确认）；若系统
存储损坏写不进，确认标记写入独立的安全存储后端，重启后不再锁死；
读取失败的异常记入日志（SL903）便于诊断；重新打开指纹锁会自动清除标记。

## 验证

- `flutter analyze` 零问题；全套测试 +543/-3（余 3 例为 Windows 本地引擎限制）。
- 构建产物核验：官方签名（`07091ffd…`）、arm64-v8a、versionCode 20、
  **插件注册 11/11 全部存在**（本地出包新增的强制验收门）。

## 已知边界

- 支持范围与限制见 [docs/SUPPORT.md](../SUPPORT.md)。
- 真机矩阵（厂商 ROM、并发、后台、soak）与截图/TalkBack 仍属用户侧验收。

## 审阅版迭代（build 21，2026-09-14 真机 v1.0.0+20 反馈）

**5. 过渡卡改为"原生盖板跟随页面状态"（口径：要么冷处理、要么盖住）**

- 真机反馈：卡片仍会露出。既然该页面的渲染结构不可完全枚举，改为
  **状态跟随式覆盖**：800ms 周期探针，只要官方页处于"配对/加载工作区"
  状态（且无任务行/输入区）或页面空白，纯图标盖板就保持覆盖；工作区
  出现即揭开。不再有 12 秒"到点必揭"。切换会话/重连时的卡片同样被覆盖。
- 误判保护："过渡卡"判定要求同时无任务行、无输入区——聊天正文中提到
  "加载工作区"字样不会被误判（用户当前正在讨论该问题）。
- 隐藏器继续保留（冷处理的第二层），双保险。

**6. 设置返回偶发"闪回设备列表页"修复**

- 根因：同屏探测只看任务行是否呈侧栏窄条；官方页打开子面板（如 web 设置）
  时列表行消失，探测判 false → 设置返回停设备列表页。
- 修复：探测增加**官方页自身的布局断点信号**（视口宽 ≥768 CSS px 即判
  同屏），与窄条信号取或；打不开子面板时宽度信号仍有效。

**7. 页内返回负向过滤（减少误点）**

- 判据补充：标签含 收起/展开/侧边/侧栏/菜单/搜索/主题/设置/折叠/更多 或
  collapse/expand/menu/search/theme/settings 的控件一律跳过；带
  `aria-expanded`/`aria-pressed` 的开关类图标跳过。
- 校验器新增 2 条行为断言（34/34）。

## 审阅版迭代（build 23 = build 22 + 验收期抓到的误点缺陷，2026-09-14）

**build 22 装机验收时又抓到一条**（这就是"必须装机验收"的价值）：

- 在平板上按返回键，脚本把 `v4-feedback-like-4` 当成了返回控件——它命中了
  `[data-testid*="back"]` 的子串（feed**back**）。后果不是"没反应"，而是
  **静默给用户消息点赞**，并把对话 scrollIntoView 滚到那条消息上。
  （用户此前反馈的"点赞动了一下但没返回"，同源。）
- 修复：
  1. testid 类选择器只认精确形态（`=back` / `-back` / `back-` / `-back-` /
     `back-button` / `goback` 等），去掉 `*="back"` 子串通配；
  2. 新增"改数据控件"黑名单（feedback / like / dislike / reaction / vote /
     favorite / undo / redo，以及 赞/踩/点赞/反馈/收藏/复制/撤销/重做），
     三条候选路径（策展选择器、通用标签、几何兜底）全部生效。
- 真机对照（活页面 /remote/v4）：
  - 工作区按返回：`{"ok":false,"reason":"not_found"}`，且 6 个反馈控件的
    aria-label 前后完全一致（无点击发生）→ 外壳据此露出设备页（用户的层级链）。
  - 设置页按返回：`{"ok":true,"reason":"clicked"}`，设置层关闭，反馈控件不变。
- 校验器补 3 条断言（反馈键不点、禁用返回键不点、精确 testid 规则仍命中
  `settings-back-button` 与 `desktop-top-nav-back`），并给沙箱补上 `$=` 选择器
  支持（此前只支持 `=` / `*=` / `^=`，会漏测精确尾匹配）；40/40。

## 审阅版迭代（build 22，2026-09-14 真机 v1.0.0+21 反馈）

本轮三个问题全部在真机上取到了**修复前 / 修复后对照实证**（800dp 平板模拟器 +
活的官方页面，经 WebView DevTools 协议），不再靠推测。

**1. 平板设置页返回"闪回设备列表页"——根因是 build 21 新增的负向过滤误杀**

- 真机命中测试（`document.elementFromPoint`）：官方设置页左上角返回键是
  `button[data-testid="settings-back-button"][aria-label="返回工作区"]`
  （rect 12,64,40,40）。两个叠加缺陷：
  1. `labelOf` 把 `data-testid` 也拼进"标签"，该 testid 含 `settings`，命中
     build 21 的负向词表 → 唯一候选被滤掉 → 脚本如实报 `not_found` →
     Dart 兜底"露出设备页"，就是用户看到的闪回。
  2. 内容签名只取**前 40 个** testid；真机设置页打开时全页共 104 个 testid，
     而设置层是追加在文档末尾的覆盖层——点击后签名不变，被判成 `no_change`
     （同一症状的另一条路径，诊断包里的 WV109 即它）。
- 修复：`labelOf` 不再包含 testid；策展选择器升级为白名单（新增官方
  `settings-back-button`，不再过负向词，仅保留"返回顶部"排除）；签名改为
  **总数 + 首 20 + 尾 20 个 testid**，设置层开合必然改变签名。
- 对照实证（同一活页面、同一场景）：
  - 修复前：`{"id":901,"ok":false,"reason":"not_found"}`，设置页仍打开。
  - 修复后：`{"id":900,"ok":true,"reason":"clicked"}`，设置页关闭。
- 负向过滤本身保留（几何 / 通用路径仍防误点），并补 2 条断言：aria-label 含
  "设置"的图标键仍必须跳过、testid 含 `settings` 的官方返回键必须被点掉。

**2. "还是有加载工作区 / 加载中还是能看到"——盖板揭开过早**

- 真机时序（文档级观察器，reload 后逐帧记录）：`+1.2s` 英文中转屏
  "Connecting relay service… / Establishing a connection between…"；
  `+2.8s` 工作区骨架 + **左栏一行 "加载中..."**（232×29）；`+5~6s` 内容到位。
- 旧规则 `(handshake && !hasContent) || blank` 有两处失明：英文中转屏不在中文
  关键词表里；`hasContent` 只看"有任务行或输入区"，而官方页的输入区在列表
  数据回来之前就先挂上了 → 盖板在左栏还挂着"加载中..."时就揭开。
- 修复：探针 v3（中文 + **英文中转屏**关键词、左栏"加载中..."占位行、被顶号
  终态）；盖板规则抽成纯函数 `shouldCoverOfficialPage`——只有"有任务行或输入区
  **且**不在任何过渡态"才揭盖；过渡态持续 20s 有超时兜底（WV113），被顶号立即
  揭盖并记 WV114（终态信息优先，让用户读到原因）。
- 误判保护：过渡卡改为"居中面板几何 + 不在聊天时间线内"识别，消息气泡里提到
  "加载工作区"不会盖住整页。真机对照实验：合成过渡卡 → `handshake=true,
  loadingRow=true`；聊天里合成同名字样 → 两者均 `false`。
- 隐藏器加一层精确规则（精确文案 + 窄小尺寸 + 左半屏）：真机对照实验侧栏占位行
  被隐藏（`display:none`），同文案的宽消息气泡不受影响。

**3. 全套测试恢复全绿（553/553）**

- 三个点击类用例此前因本机缺 `shaders/ink_sparkle.frag` 抛 "Asset not found"，
  按既有做法改用 `NoSplash` 主题（不依赖水波纹），本机与 CI 表现一致。
- 新增：盖板状态机 7 条单测（含"输入区已挂但列表仍在加载"这条真机回归）；
  注入脚本语法闸门（探针 / 隐藏器语法错误会让盖板静默失效，必须先挡住）。

**验证**

- `flutter analyze` 零问题；`flutter test` **553/553**（本机全绿）。
- `node scripts/check_injected_js.mjs` **37/37**（新增设置页返回、负向词不放宽、
  注入脚本语法闸门）。
- 真机（800dp 平板）对照实验：设置页返回点击成功、过渡态识别正确、占位行隐藏
  且不误伤消息气泡。

---

## build 24（1.0.0+24）— 安全与一致性修复版

本版来自一轮独立安全审计（b3 审计包的 R-02～R-18 逐条核实与独立复现），
不新增用户可见功能；修复集中在"安全恢复语义、凭证状态一致性、发布门禁"三条线上。

## 安全恢复（锁屏恢复不再免验证）

- **锁屏恢复改二选一，删除"确认即关闭"路径**：设备系统存储异常、指纹锁读不到
  时，用户可以 ① 用系统锁屏凭据（PIN/图案/密码）验证身份后关闭故障开关（设备
  与链接都保留），或 ② 清除本机全部远控数据后免验证进入。不再存在"点两下就
  绕过身份验证"的路径。
- **持久化失败不放行**：安全设置写不进去（prefs 与独立安全存储都失败）时保持
  锁定并给出明确提示，不谎报"已关闭保护"。
- **恢复确认面板不再依赖 Navigator**：锁屏位于整棵路由树之外，旧实现用系统
  对话框会直接报错、按钮完全不可用；现在改用锁屏内联确认面板，生产拓扑下实测
  可用（此前该路径在真实构建里打不开）。

## 数据清除（"清了磁盘、内存还在"的半完成状态）

- **新增受保护状态擦除事务**：清除数据时同步清空设备列表、活动会话、会话索引、
  连接状态、事件、遥测、桥令牌健康、待跳转目标、预热脚本（含在途延时写入），
  再清磁盘、WebView 站点数据（localStorage/IndexedDB/Cookie/缓存）与系统通知。
  事务任一步失败即保持锁定并提示重试。
- **"读不到"不再被当成"没有设备"**：安全存储枚举失败时如实报告故障（可重试），
  且不会写入空索引（旧行为会把一次临时故障固化成永久丢设备）。
- **设备变更命令串行化**：并发的新增/重命名/换链接/删除/排序不再互相覆盖；
  删除后不会被在途的重命名把凭证写回。

## 更新器与发布门禁

- 检查更新：API 超时后的网页回退不再因客户端被提前关闭而失败。
- 摘要伴随文件（`.sha256`）请求同样走逐跳白名单校验，不再自动跟随重定向。
- 发布校验要求 sidecar 为 `sha256sum` 标准行格式（摘要 + 精确文件名），与应用
  内校验、命令行 `sha256sum -c` 三方一致。
- 依赖漏洞门禁：CVSS 向量（v2/v3）现在能正确换算严重度；无法判定的条目单独
  记为"未评估"并在发布时阻断（此前高危向量会被静默放行）。
- 插件存活门：绑定同一次构建的 APK、mapping 与发布清单（空产物/换 mapping 不再
  可能通过），并接入发布流水线必需步骤。

## 其它

- WebView 换链接后同步控制器原子重建：新凭证下的状态与事件不再被静默丢弃。
- 启动盖板 20 秒兜底改为独立计时器：页面探针持续失败时也会揭盖，不再出现
  "永远的品牌图标"。

## 已知边界

- 真机项目（生物识别/相机/通知/后台/长稳）仍需实体设备验收，本版未替代。
- 版本名保持 1.0.0：已安装 1.1.x 的设备在应用内不会把本版识别为"有更新"，
  覆盖面安装升级不受影响（内部版本号继续递增）。


---

## build 25（1.0.0+25）— b4 复审整改版

来源：桌面 b4 复审报告（7.2/10 候选版）。本轮整改三项 P1（本地可闭环部分）与一项 P2：

### 桥接消息预算（R-13）

- 超限判定改为**真实 UTF-8 字节**，并在 `JSON.parse` **之前**收口——超预算正文不再
  进入解析路径（此前中文/emoji 内容字符数低于上限、字节数却可超标约 3 倍）。
- 桥未就绪时的队列同时受**字节数与条数**约束，按 UTF-8 真实字节计账。
- 分片重组增加**全局在途总额**（16 MiB）与槽位上限——多个"各自合法"的未完成分片
  不再能把内存叠加到预算之外。
- `fragmentCount` / `fragmentIndex` 必须是有限整数（小数下标被拒绝并计数）；
  `messageBytes` 声明值不再被信任（按 base64 编码长度判定）。

### 擦除事务的失败语义（R-17）

- 擦除每一步现在都产出**逐项结果**；只有**全部必需项成功**才关闭门禁，任一项
  失败保持锁定并显示可重试提示（此前站点数据/通知失败会被静默放行，与文档不符）。
- 系统通知撤销的契约明确：成功只证明**撤销请求已提交**（插件没有查询通知栏的
  API），失败按未完成处理。

### 观察面（P2）

- SSE 不再被观察：协议实测记录（`docs/RELAY-PROTOCOL-VERIFIED.md`）写明手机端为
  "4 个 REST + 2 条 WS"，没有 EventSource；观察面归零，页面行为不受影响。

### 工具链一致性

- Flutter 版本统一为 **3.47.4**（与交付 APK 的 manifest.toolchain 一致）；CI、SUPPORT
  与构建说明同步；文档防漂移检查新增"Flutter 固定版本一致"（含漂移负例）。

### 验证

- `flutter analyze` 0；`flutter test` **574/574**；注入脚本门 **47/47**；
  四个发布脚本自测 20/22/25/9；文档防漂移 8/8。
- 用 b4 复审方的受控 VM 方法复跑修复后探针：**8/8**（越界输入不进入解析/桥/内存留存）。

### 已知边界

- 仍待处理：R-14（原生 frame/origin 强校验，依赖插件版本或最小补丁）、
  R-19（requestId 级 pending，需上游协议证据）、每设备 WebView 隔离（产品决策）、
  真机矩阵 / 48h soak / 公网 OSV / CI 实跑（设备与平台依赖）。
