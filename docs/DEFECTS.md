# DEFECTS — 缺陷表

> ID 规则：`D-YYYYMMDD-NN`。严重度 P0/P1/P2/P3 定义见 `PROJECT_AUDIT.md` §3。状态：open / fixing / fixed-pending-review / closed / wontfix(需理由) / blocked / documented(已知限制成文)。
> P0/P1 不得因时间不足降级；每个 P1 修复必须附回归测试。来源：A=本轮审计 PROJECT_AUDIT；CR=零上下文 code-reviewer；SA=零上下文 security-auditor；TE=独立验收。

| ID | 级别 | 来源 | 标题 | 证据 | 复现 | 状态 | 修复 | 复审 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| D-20260916-01 | P1 | A | 同任务审批+输入并存，审批解决即清红点并撤系统通知（R-19） | `event_feed.dart`@HEAD `resolved` 直接 `remove(taskId)`；`webview_sync.dart` 无条件 `cancelPending` | `event_feed_test` "同任务审批+输入并存"在 HEAD 上失败 | **closed** | 计数化 `pendingByTask` + `pendingTotal` 携带 + 剩余>0 不撤通知 | CR 不变量 1 PASS；TE PASS（对抗 g1/g2 实证撤销条件）；复审 PASS |
| D-20260916-02 | P1 | A | 缺 `pendingInteractionSummary` 的扁平行计数缺省 0：同投递 last-wins / 跨投递覆盖基线 → 假 resolved | `TaskIndexExtractor` 计数缺省 0；`StateDiffer.apply` 原 `latestById[id]=next` | `event_parser_test` "同一投递内重复副本合并"/"跨投递"两组在旧逻辑下失败 | **closed** | `hasPendingSummary` + 同投递优先带 summary + 跨投递 `inheritPendingFrom` | CR 不变量 2/3 PASS；TE PASS；复审 PASS |
| D-20260916-03 | P2 | A | 同任务待处理计数 1→2 不再发提醒（differ 只在 0→>0 发请求事件） | `StateDiffer.apply` 转移条件 | perm 1→2 快照无事件 | **documented**（ADR-001 已知限制） | 依赖页面显式事件补位；"计数增加即提醒"需产品确认（重连重放重复通知风险） | — |
| D-20260916-04 | P2 | SA | JSON `1e999`→Infinity 后 `.toInt()` 抛异常中断整帧观察处理（先存，本轮加重依赖） | `event_observer.dart` 原 `is num`+`toInt()` 站点（计数、时间戳）；`ingestMessage` 无兜底 | 隔离探针：`jsonDecode('{"p":1e999}')` → `toInt()` 抛 `UnsupportedError` | **closed** | `_finiteInt`/`_pendingCount` 统一过滤；`ingestMessage` 外层 try/catch → BR202（已加节流） | 测试 "1e999（Infinity）/ 小数 / 字符串…"；TE 对抗 (a)(h)；复审 E/F PASS |
| D-20260916-05 | P2 | CR+SA | 负数计数通过 `is int` 门卫 → 负基线吞掉下一次 0→N 转移（漏发请求）/ 红点滞留 | 原 `if (p is int) permCount = p` | `permissionCount:-5` 后真实 1 不发 permission_request | **closed** | `_pendingCount` 只认非负 int | 测试 "负数计数按 0 处理…"；复审 B/C PASS |
| D-20260916-06 | P3 | SA | `pendingTotal` 求和可 64 位回绕为负 → 误撤系统通知 | 原 `next.permissionCount + next.userInputCount` 无封顶 | 两个 ~5e18 计数相加为负 | **closed** | 单计数封顶 `kMaxPendingCount=1024` | 测试 "超大计数封顶…"；TE 对抗 (b) |
| D-20260916-07 | P2 | CR | `EventParser.dedupe` 首见保留丢掉带计数的 differ 副本（计数记 1 而非权威 2；红点/resolved/通知不受影响） | 显式事件在前 + `dedupe` 保留首个；`_eventFrom` 无计数 | 同帧显式节点 + 镜像 perm=2 → 存活事件无 pendingTotal | **closed** | 冲突时把 `pendingTotal`（及缺失标题）合并进保留副本 | 测试 "dedupe：…合并…" + "保留副本已有标题时不被覆盖"；复审 D PASS |
| D-20260916-08 | P2 | CR | inherit 权威基线的"粘滞红点"窗口：若某 workspace 的 sessions-index 增量从此不到达而只剩扁平行，红点持续到 removed/归档/镜像重现/重启 | `StateDiffer.apply` inherit 分支；归零出口仅 summary 行与 removed | 协议推演（该场景存在性 UNKNOWN，协议证据不足） | **documented**（ADR-001 已知限制；兜底用例钉住 removed 出口） | 硬兜底"连续 N 次 inherit 后降级"有误清风险，待产品确认 | 测试 "权威基线 1 后连续只来扁平行…；removed 对账兜底清理" |
| D-20260916-09 | P3 | CR | `inheritPendingFrom` 的 `preview` 不回退基线 → 终态通知正文可能退化 | `preview: preview` | 扁平行继承后 preview 为 null | **closed** | `preview ?? baseline.preview` | 测试 "inheritPendingFrom：preview…"；复审 PASS |
| D-20260916-10 | P3 | SA | 先存：`EventDedupeGate` 键含 summary 文本，轮换 summary 可制造 0→1 振荡重复提醒 | `EventDedupeGate.keyOf` | 敌对页面轮换 summary | **closed**（iter2 W-003：键去掉 summary，改为 `type\0taskId\0`（尾段保留使 resolved 清理标记整段匹配）；同任务窗口内第二笔不同请求不再单独提醒，红点由 pendingTotal/resolved 字道校准；对抗验证：旧键回灌 → 2 条新回归 exit 1） | 可选：dedupe 键去掉 summary 或同 taskId 限频 → 采用前者 | E-22；iter2 审计 A-103/A-104 |
| D-20260916-11 | P3 | CR | prefs 关闭审批通知时 resolved(pendingTotal>0) 仍建条目（无视觉危害：红点 UI 在 unread≤0 隐藏） | `event_feed.dart` 正计数分支无条件写入 | 关闭审批提醒 → completed 建 feed → resolved(total 1) | **documented**（注释成文"计数是事实记账，与通知偏好无关"） | — | — |
| D-20260916-12 | P3 | CR2 | dedupe `sessionTitle` 合并方向无测试钉住 | `event_parser_test.dart` 原夹具两方向皆通过 | 改反方向仍通过 | **closed** | 补用例"保留副本已有标题时不被后到副本覆盖" | E-15 |
| D-20260916-13 | P3 | CR2 | `hasFlatCount` 注释"任一存在即算权威"与实现（合法非负 int）漂移 | `event_observer.dart` 注释 | 目测 | **closed** | 注释改为"合法非负 int 存在即算权威；负数/小数视为缺席" | E-15 |
| D-20260916-14 | P3 | CR2 | 兜底 BR202 日志在持续性故障下可刷穿 500 行环形缓冲 | `webview_sync.dart` catch 无节流 | 注入必抛异常 | **closed** | 首次 + 每 50 次记录并附累计数 `LogField.count` | E-15（无单测；首次路径由 TE 对抗 (h) 覆盖） |
| D-20260916-15 | P3 | CR2 | `notifier.dart notifyFrom` 内 `NotificationSpec.from` 位于内部 try 之外，`unawaited` 异步异常成 unhandled zone error 而非留痕（既有形态，非本轮引入；纯格式化风险低） | `notifier.dart` `notifyFrom` | 构造使 `NotificationSpec.from` 抛错的输入 | **closed**（iter1：spec 构造挪入独立 try，catch 记 NT501 reason=spec；回归测试"回退即失败"，对抗验证通过） | iter1 复审 B PASS | 复审 5/5 PASS |
| D-20260916-16 | P0 | A | 发布签名密钥接触面事件处置未定（治理 P0；PROJECT_AUDIT A-01）：b3 审计包曾含明文密钥 + 一次终端误输出，处置决策（保留+加固 / v3 轮换 / 全新密钥）未拍板 | `docs/PROJECT_AUDIT.md` §3 A-01；`docs/adr/ADR-003-signing-key-disposal.md` | 两次接触面事件；无证据表明流出，但无证据≠无风险 | **blocked**（等用户拍板 ADR-003；无本地可执行动作，密钥材料 AI 不接触） | 建议 A（保留+流程加固，见 ADR-003 推荐）；不轮换即维持同签名覆盖升级能力 | — |
| D-20260917-01 | P3 | CR-iter1(F-5) | 主 frame 拦截日志注释称"只记 path"，实际传 `uri?.toString()`（全 URL）——实际安全：AppLog 对 LogField.route 统一过 `LogRedactor.route` 去 query/fragment；风险在防线间接（绕过 AppLog 直接消费即泄漏） | `official_remote_page.dart` webviewNavBlocked 段；`structured_log.dart` LogRedactor.route | 目测 + 审计定位 | **closed**（iter2 W-006：改为 `uri?.path`，注释同步；复审确认 smoke 用途成立——信任判定 = 官方 origin + path 正则，误拦判别信息就是 path） | — | E-22；A-103 F-6 |
| D-20260918-01 | P3 | CR-iter2(F-5) | `onRenderProcessUnresponsive` 为 warn 级 release 可见日志，`LogField.route` 仍传 `uri?.toString()`，与 D-20260917-01 同类（防线间接）。同文件 `onLoadStart/Stop` 两处为 debug 级：release 在 `AppLog.event` 入口直接丢弃（`app_log.dart:29-32`），debug 构建下也强制过 `LogRedactor.route`——**有意保留**全 URL 以保本地 origin 诊断价值 | `official_remote_page.dart` onRenderProcessUnresponsive | 审计定位 | **closed**（iter2 随批：改 `uri?.path` + 注释；复审 PASS，debug 两处保留判定"可接受"） | — | E-22；A-104 |
| D-20260918-02 | P1 | CR-iter3(F-1) | 流程工具链：验证器 `--repo-root` 的缺陷号扫描把自身测试夹具（`tools/iteration/test_repo_links.py` 里的合成 id）当作幽灵引用，真实仓库根 `validate --repo-root .` 必然 exit 1；主代理的"exit 0"证据是在写测试文件之前跑的（时序性假绿） | `tools/iteration/iteration_state.py` `check_repo_links` | `python tools/iteration/iteration_state.py validate --repo-root . docs/continuous-iteration/ITERATION_STATE.json` → 2 条 ERROR | **closed**（跳过 `test_*.py`；单测；重跑 `valid=true`；gates.sh 新增 `state` 门每轮自动跑该校验） | 复审待确认 | A-105/A-106 |
| D-20260918-03 | P1 | CR-iter3(F-2) | `.gitignore` 整目录忽略 `tools/`，W-007"工具链入仓"实际未发生（`git ls-files tools` = 0，gates.sh 导出覆盖亦拿不到） | `.gitignore:58` | `git check-ignore -v tools/iteration/gates.sh` | **closed**（`tools/*` + `!tools/iteration/`；14 文件可见；同时 `!docs/continuous-iteration/evidence/*.log` 修 F-5 证据被 `*.log` 忽略） | 复审待确认 | A-105/A-106 |
| D-20260918-04 | P3 | MT（变异工具） | W-005 OB206 测试只喂 `droppedMessages: 3`，未测边界值 1：`evaluateApp` 阈值改为 `<= 1` 时套件仍全绿（变异 `iter3-app-bridge-drop-threshold` 存活） | `test/observer_alert_test.dart` 应用级告警组 | `mutate.py --spec mutations/iter3.json` → 12/13 | **closed**（补 `droppedMessages: 1` 断言；13/13 caught） | — | `evidence/i3-mutations.log` |
| D-20260918-05 | P1 | CR-iter4(F-1/F-4) | W-008 规模测试 11 条只有 7 条被变异钉住，批次声称"每组至少一条"与纲领"每个新测试"不符；feed 用例断言弱到 `putIfAbsent` 回退照绿；iter3 的 observer_alert_test 也有 5 条无变异（其中"负数按 0"实际守不住）。根因：变异覆盖靠口头承诺，无机器检查 | `mutations/iter3.json`、`iter4.json`；`test/scale_test.dart` feed 用例 | `mutate.py --coverage`（新）列出 6 条未覆盖 | **closed**（+8 条变异 + 2 条显式 `exempt`；feed/负数用例补强；`mutate.py --coverage` + `mutations/coverage-files.txt` + gates.sh `mutcov` 必需门；全语料 27/27 + 2 exempt） | 复核 PASS，N-1 落地 | A-107/A-108；`evidence/i4-mutations.log`、`i4-mutcov.log` |
| D-20260918-06 | P3 | CR-iter4(N-1)/MT | 语料 `expect_fail` 用 sed 改名未生效（json.dump 输出多行数组），后台全语料跑 26/27；`mutcov` 门首跑即抓到 | `mutations/iter4.json` | `mutate.py --coverage test/scale_test.dart` | **closed**（改用 json 读改写；LESSONS L-19：语料只经 json 工具改） | — | `evidence/i4-mutcov.log` |
| D-20260918-07 | P2 | SA-R3(S-1/S-2) | 输入面无界内存，页面可把影响升级为进程级击杀：① `SessionIndex` 两张表与 `StateDiffer._prev` 对唯一 sessionId 无上限（一条 4 MiB 帧 ~10 万 id，数十秒 OOM/ANR）；② 单帧 JSON 深度炸弹（~4e6 层嵌套在 `jsonDecode` 途中产生数百 MB 瞬时分配，纯 relay 帧也可触发） | `lib/state/session_index.dart`；`lib/services/event_observer.dart` StateDiffer；`lib/services/bridge_message_pipeline.dart` | 持令牌 `callHandler('zrEvents', [10 万唯一 id], token)` 循环；`'[' * (4MiB-1)` | **closed**（iter5：单设备表上限 5000、超限压到 7/8 插入序驱逐 + `_prev` 上限 10000；decode 前深度预扫 max 64（字符串内括号不计数）超限整帧丢弃；回归 `security_hardening_test.dart` 5 条 + 变异 4 条全 caught） | 复核进行中 | A-109(R3)；`evidence/i5-mutations.log` |
| D-20260918-08 | P3 | SA-R3(S-3..S-7,S-9) | 纵深防御收尾：③ hookScript 分片 assembler 原型污染（`logicalFrameId: "__proto__"`，node 实证）；④ jump/back outcome 的 `id` 用 `num` 使 Infinity/NaN 过检后 `toInt()` 抛错逃出 handler；⑤ 令牌向任意主文档无条件注入（Android POST 主框架导航不经 shouldOverrideUrlLoading）；⑥ `LogRedactor.route` 无长度上限（2 MB path × 500 行环形 ≈ 1 GB 堆）；⑦ 通知标题不截断（正文 180 对称缺失）；⑨ 安全不变量测试 UserScript 断言弱（>=3，实际 4 脚本） | `event_observer.dart` hookScript；`session_jump.dart`/`in_page_back.dart`；`official_remote_page.dart` onLoadStart/Stop；`structured_log.dart`；`notifier.dart`；`security_invariants_test.dart` | 见 `docs/audits/2026-09-18-security-r3.md` 各条构造输入 | **closed**（iter5：`Object.create(null)` + id 白名单正则（Dart 插值串内美元符转义）；`is! int`；令牌/主题只注入 `isTrustedRemotePage` 文档——无令牌文档在 `_bridgeAllowed` 已是死门，TOCTOU 失去标的；`maxRouteChars=256`；标题截 120（含省略号 121）；UserScript 计数相等且 ≥4。回归 10 条 + 变异 3 条；S-5 行为级豁免显式登记。S-8 跳转子串点击另批 → 候选 W-016） | 复核进行中 | A-109(R3) |
| D-20260919-01 | P3 | MT/iter8 | 启动器"设置"入口位于默认 600dp 测试视口底边（y≈600-621），`tester.tap` 命中不稳定：back_navigation_test 4 条用例间歇性失败（同代码跨次运行一个过一个不过） | `test/back_navigation_test.dart`；`lib/ui/manage_page.dart` ListView（真机可滚动，无用户影响） | 全量套件跨次运行对比（i8 首跑 4 失败/复跑同断言失败，i7 同代码通过） | **closed**（4 个边界用例改用 1080×2000 高视口 + addTearDown 复位；测试侧修复，App 侧无需改动——真机可滚动） | — | `evidence/i8-test.log` |
| D-20260919-02 | P2 | CR-iter8(U-1/U-2) | 大字体（200%）可达性：① 更新下载对话框不可滚动且 barrierDismissible=false → 底部"下载/安装"主按钮被裁切不可达，更新流程死路；② 设置行定高 64dp → 折行标题垂直裁切（12 处覆盖全部分区） | `lib/ui/update_download_dialog.dart`；`lib/ui/settings_page.dart` `_settingsRowHeight` | 构造：小屏 + textScaler 2.0 + 满 6 行 release notes | **closed**（① 内容包 SingleChildScrollView；② 12 处改 ConstrainedBox(minHeight)；source-pin ×2 + 变异 caught） | 复核=处方原文+机器验证（A-117） | `evidence/i8-mutations.log`；`docs/audits/2026-09-19-ui-and-entry-audit.md` |
| D-20260919-03 | P2 | CR-iter10(F-1/F-2) | iter8/9 合并批复核：① reorder 的 Future 被 ReorderCallback(void) 丢弃，存储异常成 unhandled zone error、排序失败静默回弹（DeviceStoreSuperseded 契约变更的遗留缺口，注释"均已兜底"与事实不符）；② 擦除事务漏清 deviceConnectivityProvider（remove 清了、wipe 没清，口径不一致 + clear() 死代码） | `lib/ui/manage_page.dart` onReorderItem；`lib/state/protected_wipe.dart` | 独立复核 F-1/F-2（含 Flutter SDK ReorderCallback 类型核实） | **closed**（reorder catchError + SnackBar；wipe step1 clear + coveredProviders 补录；pin 测试 ×2 + 变异 ×2 caught。附带：supersedeable 通道修双 clearAll 误报（F-3）、warmup 双 catch superseded 降级（F-4/N-1）、statusReachable 文案拆分（F-5）、DEFECTS 去重（F-7）） | 复审最终裁定可以合并 | `evidence/i10-mutations.log`；`docs/audits/2026-09-19-iter10-combined-review.md` |
| D-20260919-04 | P1 | CR-iter12(N-P1-1) | `_takeoverLogged` 只置位不重置：跨 generation/换凭证不复位，同一设备第二次被顶号时 `webviewTakeoverDetected` 永不再落日志，排障第一现场丢失 | `official_remote_page.dart:928-929` 置位处无任何重置点（grep 证实） | 状态机推导：顶号→重新配对→再顶号，第二条 warn 凭空消失 | **closed**（`_armBootCover` 按新一轮加载重置；源码钉 + 变异 iter12-takeover-reset-removed caught） | 独立复核 A-123 需返修→A-124 可合并 | `docs/audits/2026-09-19-iter12-deep-probe.md`；`evidence/i12-mutations.log` |
| D-20260919-05 | P2 | CR-iter12(N-P2-1+复核P2) | 盖板状态机：① 800ms 页面态探针揭盖后永不停（IndexedStack 永不销毁，N 台设备=N 个后台定时器，退后台空转）；② 复核加压：deadline 揭盖后横跨 20s 刻度的在途探针可把盖板重新盖上且计时器已停（弱网/离线永久品牌盖）；重挂后被陈旧探针立即揭盖 | `official_remote_page.dart` `_pageStateTimer ??=` 仅 dispose 取消；`_syncCoverWithPage` 无代次校验 | 状态机推导 + 探针时序竞态推演 | **closed**（三处揭盖点 `_disarmCoverWatch()` 停表 + `_coverEpoch` 纪元护栏：揭盖/重挂递增、陈旧探针 apply 前作废；源码钉×2 + 变异×2 caught） | 独立复核 A-123→A-124 可合并 | `docs/audits/2026-09-19-iter12-deep-probe.md`；`evidence/i12-mutations.log` |
| D-20260919-06 | P2 | CR-iter12(N-P2-1b) | OB201 的 `sseMessages` 是死输入：JS 侧从不 bump（SSE 只计 sseIgnored），留在 consumedKeys 误导维护者以为它是活告警输入 | `observer_alerts.dart` consumedKeys 含 sseMessages；`event_observer.dart:455` 只 bump sseIgnored | grep 全仓无 `bump('sseMessages')` | **closed**（迁入 ignoredKeys 并注明占位语义；evaluate 仍读；键集合护栏测试自动适配；变异 iter12-sse-key-back-to-consumed caught） | A-124 | `evidence/i12-mutations.log` |
| D-20260919-07 | P2 | CR-iter12(N-P2-2) | 连通性探测打裸 host 根路径：官方下线 /remote/vN 但保留站点根时绿点与远控页真实可用性背离 | `device_connectivity.dart` probeUri 丢 path | 探测目标与设备语义比对 | **closed**（probeUri 保留控制链接路径、去 query/fragment——凭证不随探测；测试更新钉新语义；变异 iter12-probe-root-only caught） | A-124 | `evidence/i12-mutations.log` |
| D-20260919-08 | P2 | CR-iter12(N-P2-3) | session_index 合并视图无上限：两表各 5000 独立计数，只出现在单侧的 id 使 `_rebuild` merged 可达 2×上限；附带 `_evictOverflow` 第二轮兜底 O(n²) | `session_index.dart` `_rebuild` 无驱逐 | 构造 5000+5000 互斥 id | **closed**（merged 二次 `_evictOverflow`，钉住保护不变；驱逐 Set 化 O(n)；"视图级、每轮重算"语义入注释；变异×2 caught，原 iter5 内部驱逐变异转豁免——可观察面已被合并驱逐覆盖） | A-124 | `evidence/i12-mutations.log` |
| D-20260919-09 | P2 | CR-iter12(N-P2-5) | `pendingByTask` 是状态面唯一无硬上界的表（键=页面上行 taskId，EventDedupeGate 只管 2 分钟窗口内重复），敌对页灌唯一 taskId 可无界增长 | `event_feed.dart` ingest 无 cap | 规模构造 1e4+ 唯一 taskId | **closed**（硬上限 2000、超限压 7/8、最新写入键受保护；阈值带测试钉"上限内不提前驱逐"堵"每次插入都压"的存活变异；scale 测试既有 100 任务不受影响） | A-123 需返修（阈值带）→A-124 可合并 | `docs/audits/2026-09-19-iter12-deep-probe.md`；`evidence/i12-mutations.log` |
| D-20260919-10 | P2 | CR-iter12(N-P2-6) | `normalizeVersion` 无结尾锚：`v1.2.3-rc.1` 与 `1.2.3` 同值、`v1.2.3-malicious` 参与升级比较（预发布/畸形 tag 假冒正式版） | `update_service.dart:762-768` 旧正则 | 构造带后缀 tag | **closed**（结尾锚定，带后缀 tag 一律非法 fail-closed；两段/空白兼容保留；与 W-021"更新 tag normalize"合并闭环；变异×2 caught） | A-124 | `evidence/i12-mutations.log` |
| D-20260919-11 | P3 | CR-iter12(N-P3-2) | warmup 收录带 query 的 GET：query（搜索词/分页参数）原样进安全存储并被重放，超出"只读资源路径"建档语义 | `warmup.dart` `_shouldRecord` 不查 query | 构造 `/api/v1/usage-stats?user=input` | **closed**（query/fragment 一律不收录；warmup 覆盖面小幅收窄，如实记录；变异 caught） | A-124 | `evidence/i12-mutations.log` |
| D-20260919-12 | P3 | CR-iter12+W-021 | 卫生批：① bridge_health 死条件（相邻 if 第二个包含第一个）；② `_clip`/errorText/`_boundedTitle` 三处截断可产出孤立代理对；③ alertMode/startupTarget 存储值无白名单归一；④ `AppSettings.notificationsEnabled` fail-open（未知谎报已开启）；⑤ 页面来源文本（会话标题/摘要）无 Bidi 控制符清洗；⑥ root_tabs.dart 命名失义；⑦ `lastSuccessAt` 无生产消费方 | 逐文件 grep + 构造 | **closed**（①合并单条件；②共享 `LogRedactor.clipCodeUnits`；③normalizeAlertMode/startupTarget 白名单回落；④未知一律 false；⑤TextSanitize 入 event_feed/session_index；⑥改名 pending_session_jump.dart 全仓零残留；⑦用途注释；变异×10 caught） | A-124 | `evidence/i12-mutations.log` |
| D-20260919-13 | P2 | 复核-iter12(P2-2) | 解锁窗口判定改用 Stopwatch 单调钟后，设备深睡期（suspend 不计入 CLOCK_MONOTONIC）时长冻结：息屏数小时后 `since`/`away` 几乎停在睡前值，relock 双判定全过不触发——相对旧墙钟是收紧方向的回退 | `main.dart` resumed 判定；biometric.dart Stopwatch | 场景推演：深睡后 elapsed≪relockAfter | **closed**（`BiometricService.relockEvidence = max(单调, 墙钟)`：两钟各自在深睡/回拨一种场景失真且方向互补，取大者两洞同堵；残余边界"回拨恰逢深睡"成文——设备在系统锁屏之下非独立攻击面；单测钉两场景 + 变异 caught） | A-123→A-124 可合并 | `docs/audits/2026-09-19-iter12-deep-probe.md`；`evidence/i12-mutations.log` |
| D-20260919-14 | P3 | TE/W-018b | 设备库加载的 `repaired`/`skippedRecords` 只进 AppLog：诊断页/诊断包看不到"存储发生过隔离/修复"，故障只剩日志一条短 reason | `device_store.dart` DeviceLoadResult 字段在 `_load` 处被丢弃 | 读 diagnostics_page._buildBundle | **closed**（DeviceStoreIntegrity provider + 诊断包 settings 两行 storeSkippedRecords/storeRepaired（数字/枚举）；擦除事务同生命周期清理；provider 测试 + 变异×2 caught；W-018a 附带闭环：改名/删除写失败 widget 测试×2 + 变异×2） | A-124 | `evidence/i12-mutations.log` |
| D-20260919-15 | P3 | 复核-iter13(F-2) | tools/iteration 的 unittest（验证器/仓链接检查等 31 条）从未被任何门禁执行：gates.sh selftests 段只跑 scripts/* 自测——新代码的"自测通过"声明无法被机器复验 | `tools/iteration/gates.sh:113-126`（沿袭缺口，i12 同样） | 复核员静态走查：全部 i13 证据中无该 unittest 的通过记录 | **closed**（`python -m unittest discover -s tools/iteration -p "test_*.py"` 31/31 写入 selftests 段，随 i13 门禁执行；E-49） | A-125 注记逐条闭环 | `docs/audits/2026-09-19-iter13-review.md` |
| D-20260919-16 | P2 | 路线图探索（top1） | 打开设备即 markRead 清零未读，UnreadBadge 在 unread<=0 时整体隐身——仍在等待的「待批准」红点被一并抹掉（前台无系统通知，用户再也看不到"有东西在等"） | `lib/ui/unread_badge.dart:13-14`（旧 `if (unread <= 0) return SizedBox.shrink()`）；markRead 调用点 app_shell.dart:200/225/274 | 状态推演 + 独立复核（A-126/A-127） | **closed**（未读与待批准语义分离：仅剩待批准红点常显（10px danger 圆点+语义标签），计数徽标保留；permPending 权威记账未动；6 变异钉 6 用例；event_feed 注释同步更新） | 复核 A-126 需返修（语料问题）→ A-127 终裁 | `docs/audits/2026-09-19-iter14-review.md` |
| D-20260921-01 | P2 | iter16 清单 | 生产 seed 路径从不写 `deviceStoreIntegrityProvider`：诊断页/诊断包恒报 storeSkippedRecords=0 / storeRepaired=no（W-018b 声称的闭环在主路径上不成立） | `main.dart` seed 构造绕过 `_load()`（唯一 report 调用点）；`diagnostics_page` `?? 0/?? false` 把"未知"渲染成"干净" | 擦除事务删索引键 → 下次启动 repaired=true 而诊断面显示 no（路径推导） | **closed**（main 用 `DeviceStoreIntegrityNotifier(initial:)` 注入首帧前加载结果；诊断包 null 渲染 `unknown`；provider 注入/unknown 两向测试 + 变异×3。复核返修：`unavailable`（读取失败）注入/上报 null=unknown，不再落成 0/no 的"干净"读数，测试 + 变异×4） | 本轮门禁 + 变异机器验证；终审 6 项返修闭环（iter16 复核返修）；独立复审待下轮 | `tools/iteration/mutations/iter16.json` |
| D-20260921-02 | P3 | iter16 清单 | 仓库迁移漏改 SECURITY.md/README.en.md：私有漏洞报告入口与 Releases 下载仍指向已封停账号（HTTP 404），DEC-17「文档全部同步改址」不成立 | `SECURITY.md:5`、`README.en.md:27` 原文；`git grep 2421873411a-rgb` | WebFetch advisories/new → 404（昨测） | **closed**（两处改址 daniei-chen；源码级回归测试 + 变异） | 本轮门禁 + 变异机器验证；终审 6 项返修闭环（iter16 复核返修）；独立复审待下轮 | `tools/iteration/mutations/iter16.json` |
| D-20260921-03 | P2 | iter16 清单 | 删除设备不撤销该设备的系统通知（payload 持 device/session id）；`_jumpTo` 对已删设备仍写 pendingSessionJump（长驻、无消费方） | `session_pool.remove` 清理清单无通知撤回；`app_shell._jumpTo` `if (index>=0)` 之外无条件 set | 代码路径推导 | **closed**（`NotifierService.cancelForDevice` 撤销四类通知位；remove 同时清指向该设备的在途跳转；`_jumpTo` 设备不存在提前返回；通知通道级测试 + 变异×3。复核返修：`notifyFrom` show 成功后**持久登记**通知 id（DeviceStore，64/设备有界，擦除同清），cancelForDevice 先读登记再并入内存兜底——重启后（feed/history 为空）与无 taskId 的通知也撤得掉；枚举路径经核（插件 22.3.0 原生 `getActiveNotifications` 不返回 payload）不采用，注释如实说明；测试 + 变异×5） | 本轮门禁 + 变异机器验证；终审 6 项返修闭环（iter16 复核返修）；独立复审待下轮 | `tools/iteration/mutations/iter16.json` |
| D-20260921-04 | P3 | iter16 清单 | 凭证指纹键集合缺 `remoteControlToken`（应用自有 token 键名）且测试夹具用 `t` 冒充掩盖缺口：只带 remoteControlToken 的链接指纹为 null，同链接扫两次生成两台设备（F24 残余面） | `credential_fingerprint.dart:20` vs `link_builder.dart:157`；`device_import_test` 夹具 | 构造 remoteControlToken-only 链接 | **closed**（键集合补 `remotecontroltoken`；夹具改应用真实键名 + 显式去重用例；变异（去键）） | 本轮门禁 + 变异机器验证；终审 6 项返修闭环（iter16 复核返修）；独立复审待下轮 | `tools/iteration/mutations/iter16.json` |
| D-20260921-05 | P3 | iter16 清单 | 三处裸 UTF-16 码元截断（`_eventText`/`_previewText` 180、通知 `_clip` 120/180）可切出孤立代理对（半 emoji），未随 W-021 迁到共享 `clipCodeUnits` | `event_observer.dart:734/1042`、`notifier.dart:93` 旧实现 | 构造 179×'a'+emoji 输入 | **closed**（三处迁 `LogRedactor.clipCodeUnits`；两条行为测试 + 变异×3） | 本轮门禁 + 变异机器验证；终审 6 项返修闭环（iter16 复核返修）；独立复审待下轮 | `tools/iteration/mutations/iter16.json` |
| D-20260921-06 | P3 | iter16 清单 | 存储策略清单/policySummary 声称「移除设备」清站点数据，实际删除路径不清理：诊断页/诊断包展示的存储策略与实现不符 | `webview_storage.dart:18/22/27` vs 全仓调用点 grep（仅换凭证/擦除） | 目测 + 调用点核对 | **closed**（文案按实现收紧为「换凭证/锁定擦除」——origin 共享，单台删除无法只清一份且全清会波及其余设备；ADR-004 背景加更正；测试 + 变异。复核返修：用户可见的 docs/PRIVACY.md 同步收紧（上轮只改了 in-app 清单、漏了这份 README 可达文档），测试 + 变异×3） | 本轮门禁 + 变异机器验证；终审 6 项返修闭环（iter16 复核返修）；独立复审待下轮 | `tools/iteration/mutations/iter16.json` |
| D-20260921-07 | P3 | iter16 清单 | official_remote_page 桥回调在 await 后缺 mounted 守卫（`_markBridgeTokenReady`/`_logTokenMissing`/zrViewState/zrSeen/zrStats/zrWs）：卸载后 `ref.read` 抛 StateError（口径与 zrEvents 不一致） | 各 handler 读码；riverpod `_assertNotDisposed` | 设备在页面存活期被删/换凭证 + 回调在途（窗口窄，未实测） | **closed**（六处补 `mounted`/`context.mounted` 同口径守卫；桥令牌预算改双钟 `BridgeTokenPolicy.waitBudgetExhausted`；双钟纯函数测试 + 守卫源码钉（iter16 复核返修补齐，回调路径无法在 widget 测试里驱动故用源码钉）+ 变异×5） | 本轮门禁 + 变异机器验证；终审 6 项返修闭环（iter16 复核返修）；独立复审待下轮 | `tools/iteration/mutations/iter16.json` |
| D-20260921-08 | P3 | iter16 清单 | 待处理中心等待行在历史无命中时点按无可见导航：跳转目标只从 50 条历史取，feed 权威键未作兜底（点按停在原地） | `pending_center_sheet.dart:203-208`；`event_history.dart:103-115` | 构造：pendingByTask 命中但历史无该任务 | **closed**（未命中时用 feed 权威键兜底，well-formed 且排除 'unknown' 占位；widget 测试 + 变异） | 本轮门禁 + 变异机器验证；终审 6 项返修闭环（iter16 复核返修）；独立复审待下轮 | `tools/iteration/mutations/iter16.json` |
| D-20260921-09 | P3 | iter16 清单 | 连通性遥测：换凭证不清旧探测结果（probeUri 随 path 变化）；在途轮次在 remove 的 forget 之后把已删设备写回 provider | `device_connectivity.dart:140-144` 注释 vs 调用点；`probeAll` 写回 | 构造在途轮次 + 删除 | **closed**（replaceLink 补 forget；轮内 forget 标记使在途写回跳过；两条行为测试 + 变异×2。复核返修：`clear()`（擦除事务）同样作废在途轮次（`_wipedInFlight`），测试 + 变异×3） | 本轮门禁 + 变异机器验证；终审 6 项返修闭环（iter16 复核返修）；独立复审待下轮 | `tools/iteration/mutations/iter16.json` |
| D-20260921-10 | P3 | iter16 清单 | activeSession 通道无生产消费方，却每条桥消息做深度 6 整树扫描并写 provider（观测热路径纯开销 + 无人读的状态面） | `webview_sync.dart:79-83/189-190`；NotificationGate.shouldNotify 参数被忽略 | 全仓 grep 消费方 | **closed**（删除整树扫描与 provider 写入，保留轻量字段；源码钉 + 变异） | 本轮门禁 + 变异机器验证；终审 6 项返修闭环（iter16 复核返修）；独立复审待下轮 | `tools/iteration/mutations/iter16.json` |
| D-20260921-11 | P3 | iter16 清单 | zrSeen 预算跨层不一致：钩子单批可达 ~1 MiB，桥侧 512 KiB/接收侧 256 KiB 会整批丢弃（诊断面报应用级 OB206 桥丢弃告警） | `event_observer` 钩子 64×16 KiB；`bridge_schema.dart:26`；`warmup.dart` 旧 256 KiB | 按预算推算（需单窗口 64 条大 body） | **closed**（新增 `kMaxSeenBatchBytes=192 KiB`：钩子按 UTF-8 字节分批冲刷、warmup 第二道门同常量、桥侧留 64 KiB 余量；node JS 门新增行为断言 + Dart 边界测试 + 变异×2） | 本轮门禁 + 变异机器验证；终审 6 项返修闭环（iter16 复核返修）；独立复审待下轮 | `tools/iteration/mutations/iter16.json` |
| D-20260921-12 | P3 | iter16 清单 | 两处墙钟时限：EventDedupeGate 窗口/压制判定与桥令牌等待预算——系统时间回拨使旧键不过期 + 新事件被静默压制 / 等待被拉长 | `event_observer.dart:1706-1735`；`official_remote_page._ensureBridgeToken` 旧 deadline | 状态推演 | **closed**（闸门默认时钟改单调（进程起点 + Stopwatch，注入接口保留）；令牌预算双钟取大；测试×2 + 变异×2） | 本轮门禁 + 变异机器验证；终审 6 项返修闭环（iter16 复核返修）；独立复审待下轮 | `tools/iteration/mutations/iter16.json` |
| D-20260921-13 | P3 | iter16 清单 | `WarmupMemoryNotifier.load` 在 await 后无守卫：擦除 clearAll 之后到达的存储读结果把已清除设备的预热脚本写回内存（与同类 notifier 口径不一致） | `warmup.dart:86-97`（旧） | 构造慢读 + clearAll | **closed**（`ref.mounted` + 擦除纪元 + **每设备纪元**三重守卫：clearAll 推全局纪元、forget（设备删除）推该设备纪元，在途读跨越任一边界即作废；行为测试×2 + 变异×3） | 本轮门禁 + 变异机器验证；终审 6 项返修闭环（iter16 复核返修）；独立复审待下轮 | `tools/iteration/mutations/iter16.json` |
| D-20260921-14 | P3 | iter16 清单 | 通知深链 payload `deviceId|taskId` 未转义：页面可控 taskId 含 `|` 时 `parts[1]` 取到前缀，可能命中同前缀的另一个会话 | `notifier.dart:159-160`；`app_shell` split；`session_jump` 白名单 | 构造 'dev|abc|def' | **closed**（`SessionJump.decodePayload` 精确两段 + 白名单，调用方统一走它；单测 + 变异） | 本轮门禁 + 变异机器验证；终审 6 项返修闭环（iter16 复核返修）；独立复审待下轮 | `tools/iteration/mutations/iter16.json` |
| D-20260921-15 | P3 | iter16 清单（MT） | 语料幽灵豁免：iter7.json 豁免条目 covers 的测试名「扫码写失败复位与提示」全仓不存在——R-1 扫码写失败复位路径无 widget 测试/变异，豁免声明落空 | `iter7.json` 该条目；`rg 扫码写失败` 全仓仅命中语料自身 | 全仓 grep 0 命中测试 | **closed**（补 `_acceptCode` 拆分 + debug 入口 + widget 行为测试（写失败复位后可再次进入写路径）+ 变异；删除幽灵豁免条目） | 本轮门禁 + 变异机器验证；终审 6 项返修闭环（iter16 复核返修）；独立复审待下轮 | `tools/iteration/mutations/iter16.json` |
