# DECISIONS — 决策与假设记录

> 每条：背景 / 选项 / 决定 / 理由 / 后果 / 复审条件。重大架构决策另立 `docs/adr/ADR-xxx.md`。

| ID | 日期 | 背景 | 选项 | 决定 | 理由 | 后果 | 复审条件 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| DEC-01 | 2026-09-16 | 项目输入卡未填 | 追问 / 保守假设 | **保守假设**：项目=当前仓库；模式=规划并执行（用户随后明确）；预算=9h（模板示例）；允许联网只读查官方资料与 OSV；允许安装依赖（pub get）；不部署/不付费/不发消息/不推送 | 母提示词规定"未填写按保守可逆假设并记录" | 时间盒按 9h 分配；实际以有效工作量为准 | 用户给出真实预算或约束时 |
| DEC-02 | 2026-09-16 | 基线取证要跑 `flutter test`，但台账记录其会重写未跟踪生成文件 | 真实工作区跑 / 临时导出跑 | **临时导出**（`D:\tmp\zr\baseline_head_16a7d8e`）跑基线 | 不给真实工作区留"下次出包坏包"的陷阱；HEAD 导出天然排除未提交改动，基线纯净 | 真实工作区跑测试须备份/恢复生成文件 | 生成文件纳入版本控制或构建脚本自动重生时 |
| DEC-03 | 2026-09-16 | 四份治理文档放置位置 | 仓库根 / `docs/` | **`docs/`** | 仓库既有约定：计划与台账都在 `docs/`（`PLAN-v1.1.0.md`） | 未提交，用户决定是否入库 | — |
| DEC-04 | 2026-09-16 | R-19 审计要求 pending 按 (taskId, requestId)，需上游证据 | 请求级 id / 按任务计数 | **按任务权威剩余计数降级**（ADR-001） | 观察面证据：`pendingInteractionSummary` 只有聚合计数；`pendingInteraction.interactionId` 只标识当前浮出的一条，无法枚举其余挂起项 | 红点/通知语义按剩余量；1→2 不再提醒成为已知限制（D-03） | 上游若提供全部挂起交互列表 |
| DEC-05 | 2026-09-16 | 半成品 b6 审计包 staging 已建，用户改口不需要 | 保留 / 删除 | **删除 staging，源码导出留作基线目录** | 半成品可能被误认为可交付物；导出内容仍有取证价值 | b6 未产出；b5 包仍为最新交付 | 用户再次要求出包 |
| DEC-06 | 2026-09-16 | 本轮是否提交 | 提交检查点 / 不提交 | **不提交、不推送** | 未获用户明确要求；分支为 release 分支 | 结束时报告 `git diff --stat`，由用户决定 | 用户要求提交 |
| DEC-07 | 2026-09-16 | 复审 P3-4：`notifier.dart` 内 `NotificationSpec.from` 位于 try 之外（既有形态） | 本批顺手修 / 留下一批 | **留下一批**（D-20260916-15） | 超出本批文件范围；纯格式化函数风险低；本批已三轮审计收口，避免扩大改动面重开复审 | 异步格式化异常仍是 unhandled zone error（不影响记账状态） | 下一批改动 notifier.dart 时 |
| DEC-08 | 2026-09-16 | 兜底 BR202 日志是否节流 | 每帧记录 / 首次+周期 | **首次 + 每 50 次附累计数** | 持续性故障下每帧记录会刷穿 500 行环形缓冲、淹没其他诊断；周期记录仍能定位首因与规模 | 第 2～49 次丢帧不单独留痕 | 诊断页若增加丢帧计数器则可进一步简化 |
| DEC-09 | 2026-09-16 | 独立验收首次因账号速率限制失败 | 放弃 / 重派 | **单代理重派**（不再三代理并发） | 验收是闸门必需项，不能以"其他两道通过"替代 | 审计阶段耗时增加；结论完整 | — |
| DEC-10 | 2026-09-16 | 用户要求以 continuous-iteration-plan 技能接管，模式未指定 | 执行 / 仅规划 / 只要模板 | **仅规划模式**（用户拍板） | 用户明确"只规划、不改业务代码、先看方案再决定"；技能规定规划模式不得扩成施工授权 | 本回合只生成控制面文件与执行任务书，不动业务代码；施工需用户设 /goal 后由后续回合执行 | 用户改口要执行模式时 |
| DEC-11 | 2026-09-16 | 控制面目标成熟度档位 | prototype / internal_beta / release_candidate / production_improvement | **release_candidate（85）**（用户拍板） | 与仓库现状匹配：CI 实跑/真机矩阵/密钥处置三项外部阻塞使 95 档长期不可达；85 档下剩余项可转合规延期 | T2/T4 在外部与用户决策项解除前不可 DONE；总分 <85 或任一维度低于档位下限不 TERMINATED（当前 79：security 70.8%、operations 16.7% 是主缺口） | 用户给出更强预算与外部资源时 |
| DEC-12 | 2026-09-16 | 控制面预算 | 无预算 / 轮次 / 时间 | **无预算**（用户拍板） | 技能规定只有用户明确预算才产生 BUDGET 终态；避免自设时钟造成假终态 | 依靠目标范围收敛与合规延期收敛候选池 | 用户给出明确上限时写入 budget 字段 |
| DEC-13 | 2026-09-16 | 状态机控制目录选址 | 仓库根 / `docs/continuous-iteration/` | **`docs/continuous-iteration/`** | 技能默认值；与既有 `docs/` 台账同域，README 防漂移检查不覆盖新增目录 | 新增 1 目录；`source_map` 指向既有 DEFECTS/EVIDENCE/DECISIONS/ACCEPTANCE_MATRIX，不建平行台账 | — |
| DEC-14 | 2026-09-16 | 基线门禁在哪跑（与 DEC-02 同因） | 真实工作区 / 临时导出 | **临时导出 `/d/tmp/zr/ci_planning_20260916`**（含工作区改动与未跟踪文件的完整快照） | 与 DEC-02 同因：`flutter test` 会重写未跟踪生成文件 `GeneratedPluginRegistrant.java`；导出目录内容 = 工作区实际内容，基线不失真 | E-17 证据路径在导出目录；真实工作区生成文件保持 release 形态 | — |
| DEC-15 | 2026-09-16 | P2 条目 C7/D5 与 D-03/D-08 硬兜底是否延期/改动 | 延期 / 原地维持 | **C7/D5 维持 BLOCKED 不延期；D-03/D-08 维持 documented（ADR-001 已知限制），硬兜底属目标外改进不列入候选** | P2 延期需用户明确接受（技能规则），当前无此授权；硬兜底"连续 N 次 inherit 后降级"有误清风险，需产品确认，且不属于 release_candidate 目标必需项 | 阻塞项按 0 分计入分母；T2 在拍板前不可 DONE；硬兜底问题保留在 ADR-001 复审条件中 | 用户拍板 ADR-004/005、或用户提出改硬兜底时 |
| DEC-16 | 2026-09-16 | 验收矩阵新增计分属性（维度/权重/关键）的冻结时点 | 立即冻结 / 施工中冻结 | **控制面初始化即冻结**（revision 1） | 技能要求初始化后冻结 ID、权重与适用性；后续新增真实发现可增加分母，但删除/改权重须记录 change_log | 后续轮次改动需写入 `ITERATION_STATE.json` 的 `change_log` 并附审阅证据 | — |
| DEC-17 | 2026-09-19 | 原账号 2421873411a-rgb 被 GitHub 封停（push 实测 403 "account suspended"，gh 令牌同因失效），用户重新授权无效 | 申诉解封 / 换新账号 | **换新账号**：仓库迁移至 `daniei-chen/ZCode-App`，App 内更新链/文档/CI 全部同步改址（12 文件），受影响测试 73/73 绿 | 解封周期不可控；新仓库即时可用 | 旧 Release 历史不迁移（如需从新仓库重发由用户决定）；`releases/latest` 更新检查指向新仓库后，需在新仓库发布 Release 才能返回版本 | 永久（除非用户换回） |
| DEC-18 | 2026-09-19 | tag 策略待决（v1.0.0 已被历史占用，W-BL5/BL-006） | 新 tag 命名线（v1.1.x）/ 沿用 v1.0.0 | **沿用 v1.0.0：最新一版即 v1.0.0，内部 versionCode 递增**（+28）；tag 重指到当前 HEAD | 用户拍板"按照最新一版为 1.0.0"；与 ADR-003 方案 A 的既有发布链一致 | 更新比较逻辑不变（versionName 恒 1.0.0 时 `hasUpdate` 不触发跨版本提示，更新交付走 GitHub Release 资产覆盖安装） | 用户要求开启多版本线时 |
